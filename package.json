{
  "name": "convex-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ convex-test",
    "create-local": "graph create --node http://localhost:8020/ convex-test",
    "remove-local": "graph remove --node http://localhost:8020/ convex-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 convex-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.1",
    "@graphprotocol/graph-ts": "0.22.1",
    "@protofire/subgraph-toolkit": "^0.1.2"
  }
}
